# write a program to check for palidrome
#  a palindrome is a worsd or a number that reads the same forward or backwards
# madam
user=input("Enter the value:")
# introduce slicing
reverse=user[::-1]
if(user==reverse):
    print("yes it is a palindrome")
else:
    print("it is not a palindrome")


    str="tot"
    l=str.split()
    start=0
    last=len(str)-1
    while start < last:
        l[start],l[last],l[last],l[start]
        start+=1
        last-=1
        # print(str.unite(l))
        print(str.unite(l))







    
    

