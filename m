from typing import List


list=[20,50,70,90]
list.remove(min(list))
print(list)

List=[30,60,70]
list.remove(min(list))
print(list)


list=['joel','faith','anjelka']
len(list)
print(list)
# list=(len[0])
# print(list)

listA = ["Jeff", "Bezos", "Is", "Retiring", "from", "Amazon"]

print("The length of the list is:", len(listA))
